package fp.figuras;

public interface Cuadrado extends Figura{

	
	/**
	 * @param nuevoLado Nuevo tama�o en pixels.
	 * Cambia el tam�ao por el nuevo tama�o (en pixels). El tama�o debe ser >=0.
	 */
	void cambiarTamanyo(Integer nuevoLado);

}